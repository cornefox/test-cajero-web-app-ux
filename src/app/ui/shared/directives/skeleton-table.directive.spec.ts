import { SkeletonTableDirective } from './skeleton-table.directive';

describe('SkeletonTableDirective', () => {
  it('should create an instance', () => {
    const directive = new SkeletonTableDirective();
    expect(directive).toBeTruthy();
  });
});
